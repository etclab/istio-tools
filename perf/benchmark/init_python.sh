pip install --upgrade pip && pip install pipenv
~/.local/bin/pipenv shell
~/.local/bin/pipenv install
# after calling pipenv shell, the below commands are skipped
# pip install pipenv
# pipenv install